# 🔒 Web Application Security Testing – DVWA Project

This project demonstrates penetration testing on the **Damn Vulnerable Web Application (DVWA)** using **OWASP ZAP** and **Docker**.

---

## 🧠 Tools Used
- Windows 10 / 11
- Docker Desktop
- OWASP ZAP
- Firefox Browser

---

## ⚙️ Setup & Configuration
1. Install **Docker Desktop** on Windows.
2. Pull & Run the DVWA container:
   ```bash
   docker run --rm -it -p 80:80 vulnerables/web-dvwa
